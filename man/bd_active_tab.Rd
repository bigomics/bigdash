% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility.R
\name{bd_active_tab}
\alias{bd_active_tab}
\title{The tab this board is in, and whether it is the open one}
\usage{
bd_active_tab(
  id = BIGDASH_DEFAULT_ID,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{Namespace id of the \code{\link[=bigTabs]{bigTabs()}} instance, as given in the UI. If
the calling module's namespace already carries that prefix (the usual shape
for a nested \code{bigTabs(id = id)}), it is stripped, so the same call works
from the app server and from inside the module.}

\item{session}{Session to read the nav input from.}
}
\value{
A \code{reactive} returning the open tab's name.
}
\description{
\code{bd_active_tab()} returns the name of the \code{\link[=bigTabItem]{bigTabItem()}} currently open in a
\code{\link[=bigTabs]{bigTabs()}} instance, straight from the nav input bigdash already maintains
-- no probe, no JavaScript, no round trip, and never \code{NULL} after the first
navigation.
}
\details{
It is the cheap alternative to \code{\link[=bd_is_visible]{bd_is_visible()}} and the right one when tab
selection is all you need. It does not compose across nesting: a board in a
\code{\link[=bigTabs]{bigTabs()}} nested inside another one is on screen only if the outer tab is
open too, which this cannot see. Use \code{\link[=bd_is_visible]{bd_is_visible()}} when that matters, or
when visibility is decided by an accordion, a \code{bslib} navset, or scrolling.
}
\examples{
\dontrun{
active <- bd_active_tab()
is_visible <- shiny::reactive(active() == "alpha-tab")
}

}
