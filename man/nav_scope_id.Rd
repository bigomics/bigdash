% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigation.R
\name{nav_scope_id}
\alias{nav_scope_id}
\title{Resolve the \code{\link[=bigPage]{bigPage()}} id \code{session} belongs to, for scoping nav messages.}
\usage{
nav_scope_id(session)
}
\description{
\code{session$ns(NULL)} is \code{""}/\code{character(0)} for the top-level session and
the module id for a \code{moduleServer()} session_proxy -- which is exactly the
\code{id} convention \code{\link[=bigPage]{bigPage()}}/\code{\link[=sidebar]{sidebar()}}/\code{\link[=settings]{settings()}} already use (see
\code{\link[=scoped_id]{scoped_id()}}). Returning \code{NULL} for the top-level case (rather than \code{""})
matters: \code{send_nav()} treats a \code{NULL} value as "no value field at all",
which the JS side's \code{scopedId()} then reads as the unscoped default,
matching pre-existing, page-wide behaviour exactly.
}
\keyword{internal}
