% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{scoped_id}
\alias{scoped_id}
\title{Scope an internal element id to a \code{\link[=bigPage]{bigPage()}} namespace.}
\usage{
scoped_id(id, suffix)
}
\arguments{
\item{id}{Namespace, as passed to \code{\link[=bigPage]{bigPage()}} and friends.}

\item{suffix}{Element suffix, e.g. \code{"sidebar-container"}.}
}
\description{
When \code{id} is the default (\code{"app"}), the plain \code{suffix} is returned so
the generated markup and ids are unchanged from previous releases.
For any other \code{id}, the ids are prefixed so that multiple \code{\link[=bigPage]{bigPage()}}
(or \code{\link[=sidebar]{sidebar()}} / \code{\link[=bigTabs]{bigTabs()}} / \code{\link[=settings]{settings()}} / \code{\link[=sidebarHelp]{sidebarHelp()}}) can be
nested on the same page without colliding, e.g. when one bigdash
dashboard is embedded as a tab/module inside another.
}
\keyword{internal}
