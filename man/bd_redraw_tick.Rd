% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility.R
\name{bd_redraw_tick}
\alias{bd_redraw_tick}
\title{Redraw tick for plots purged while hidden}
\usage{
bd_redraw_tick(
  is_visible = NULL,
  session = shiny::getDefaultReactiveDomain(),
  label = NULL
)
}
\arguments{
\item{is_visible}{Reactive logical, defaulting to the one \code{\link[=bd_is_visible]{bd_is_visible()}}
registered for the enclosing board.}

\item{session}{Module session.}

\item{label}{Optional label; when given, purge and redraw events are
\code{message()}d -- handy for confirming purging is actually happening.}
}
\value{
A \code{reactive} returning an integer.
}
\description{
While a board is off screen its drawn Plotly/iheatmapr trees are dropped (see
\code{\link[=bd_visibility_probe]{bd_visibility_probe()}}). The plots are then only blank because we emptied
them, and Shiny will not resend a value for an output it has not invalidated
-- so anything purged needs a dependency that changes when the board comes
back. This is that dependency.
}
\details{
\code{\link[=PlotModuleServer]{PlotModuleServer()}} wires this up itself. Use it directly for plots rendered
outside a plot module, by reading \code{tick()} in the render expression or by
wrapping the render function in \code{\link[=bd_with_redraw]{bd_with_redraw()}}.

The tick only moves after a purge actually happened, so a first visit -- or a
board whose \code{purge} is switched off -- renders exactly once.
}
\examples{
\dontrun{
redraw <- bd_redraw_tick()
output$scatter <- plotly::renderPlotly({
  redraw()
  plotly::plot_ly(df, x = ~x, y = ~y)
})
}

}
