% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility.R
\name{bd_visibility_probe}
\alias{bd_visibility_probe}
\title{Probe reporting whether this board is on screen}
\usage{
bd_visibility_probe(ns, spinner = TRUE)
}
\arguments{
\item{ns}{Module namespace function, \code{shiny::NS(id)}.}

\item{spinner}{Keep the \code{\link[bigLoaders:useSpinner]{bigLoaders::useSpinner()}} spinner up until the
widget has actually painted, instead of hiding it when the data arrives.
Matters most right after a purge, when the whole redraw is client-side.}
}
\value{
A \code{\link[shiny:tagList]{shiny::tagList()}} to prepend to the board UI.
}
\description{
Prepend to a board's UI. It reports whether the board is actually shown --
its tab selected, its accordion open, its parent app tab active -- to the
board's server as \code{input$is_visible}, read with \code{\link[=bd_is_visible]{bd_is_visible()}}.
}
\details{
The check is generic (an \code{IntersectionObserver} plus an \code{offsetParent} test
on an invisible 1px element) rather than tied to any one tab implementation,
so it composes: a board nested in a \code{\link[=bigTabs]{bigTabs()}} inside another \code{\link[=bigTabs]{bigTabs()}}
is visible only when both are.

Enabling the probe also lets bigdash drop the drawn Plotly/iheatmapr trees
of a hidden board -- see \code{\link[=bd_redraw_tick]{bd_redraw_tick()}} and the \code{purge} argument of
\code{\link[=PlotModuleServer]{PlotModuleServer()}}. Nothing is dropped until \code{\link[=bd_is_visible]{bd_is_visible()}} switches it
on from the server, because a purged plot only comes back if something holds
a redraw tick for it.
}
\section{Keep it in flow}{

The probe must stay in normal layout flow -- do not put it inside something
\code{display:none} of your own, or it can never register as visible. It is
transparent rather than hidden for that reason.
}

\examples{
boardUI <- function(id) {
  ns <- shiny::NS(id)
  shiny::div(
    bd_visibility_probe(ns),
    plotly::plotlyOutput(ns("scatter"))
  )
}

}
\seealso{
\code{\link[=bd_is_visible]{bd_is_visible()}}, \code{\link[=bd_redraw_tick]{bd_redraw_tick()}}, \code{\link[=bd_active_tab]{bd_active_tab()}}
}
