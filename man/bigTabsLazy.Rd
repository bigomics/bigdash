% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tabs-lazy.R
\name{bigTabsLazy}
\alias{bigTabsLazy}
\title{Load a tab's UI and server the first time it is opened}
\usage{
bigTabsLazy(
  tabs,
  id = BIGDASH_DEFAULT_ID,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{tabs}{Named list, one entry per tab, named by the tab's \code{name} as
given to \code{\link[=bigTabItem]{bigTabItem()}} -- the fully-namespaced name (e.g.
\code{session$ns("plots-tab")}), not the bare one, unless \code{id} is the default
\code{"app"}. A name that doesn't start with that namespace is rejected up
front, since otherwise nothing loads and nothing errors. Each entry is a
list with \code{ui} and/or \code{server}, each a function of no arguments; either
may be omitted. Set \code{preload = TRUE} on an entry to load it immediately
instead of waiting for its tab to be opened -- for a board whose server
other boards depend on, which therefore cannot wait for a click.}

\item{id}{Namespace id, matching the enclosing \code{\link[=bigPage]{bigPage()}}.}

\item{session}{Shiny session; defaults to the current one.}
}
\value{
Invisibly, a function returning the names of the tabs loaded so far.
}
\description{
A \code{\link[=bigTabItem]{bigTabItem()}} normally carries its whole board: the markup is built and
the module server started before the user has been anywhere near that tab.
With many tabs that is the dominant cost of opening a dashboard, and most of
it is for tabs nobody visits.
}
\details{
\code{bigTabsLazy()} defers both halves. Give it, per tab, a function returning
the UI and a function starting the server; on the first activation of that
tab it inserts the UI, then calls the server, then never again.

Leave the tab's heavy content out of \code{\link[=bigTabItem]{bigTabItem()}} and keep only what must
exist up front there -- typically the sidebar inputs and a placeholder.
}
\section{Ordering}{

The UI is inserted before the server function runs. Module servers routinely
call \code{updateSelectInput()} and friends during initialisation, and those
messages are dropped if the elements are not in the DOM yet.
}

\section{Scoping}{

Activation is read from the \emph{scoped} nav input (\verb{<id>-nav}, or \code{nav} for the
default instance), so a nested \code{\link[=bigPage]{bigPage()}} cannot trigger its parent's tabs
through a tab-name clash. The UI is inserted with a selector rooted at this
instance's \verb{#<id>-big-tabs} for the same reason.
}

\examples{
\dontrun{
## ui
bigTabs(
  bigTabItem("plots-tab", plotsInputs("plots"))  # inputs only
)

## server
bigTabsLazy(list(
  "plots-tab" = list(
    ui     = function() plotsUI("plots"),
    server = function() plotsServer("plots", data = data)
  )
))
}

}
