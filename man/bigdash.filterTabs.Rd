% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/navigation.R
\name{bigdash.filterTabs}
\alias{bigdash.filterTabs}
\title{Filter visible tabs and menu items}
\usage{
bigdash.filterTabs(session, tabs, id = BIGDASH_DEFAULT_ID)
}
\arguments{
\item{session}{Shiny session.}

\item{tabs}{Character vector of tab names (\code{data-name} / \code{target} values
from \code{\link[=bigTabItem]{bigTabItem()}}, \code{\link[=sidebarItem]{sidebarItem()}}, etc.).}

\item{id}{Namespace id of the \code{\link[=bigPage]{bigPage()}} instance to filter, as given to
\code{\link[=bigPage]{bigPage()}}/\code{\link[=bigTabs]{bigTabs()}}/\code{\link[=sidebar]{sidebar()}} in the UI. Only needed when several
\code{bigPage()}s render in parallel from the same server, each with its own
\code{id} (not nested) -- omit it for the default, single-\code{bigPage()} case.}
}
\description{
Shows only the specified tabs (and their corresponding sidebar menu items).
Hides all other menu items and their associated tab canvases (\code{big-tab}
elements). Useful for dynamic filtering of available boards/views.
}
\details{
The currently active tab stays selected if it is still among \code{tabs};
otherwise the first tab in \code{tabs} is selected. This avoids bouncing the
user back to \code{tabs[[1]]} on every call when their current tab was never
actually filtered out.
}
