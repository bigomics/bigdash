% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table-module.R
\name{as_dt_widget}
\alias{as_dt_widget}
\title{Coerce to a \code{\link[DT:datatable]{DT::datatable()}} widget if it isn't already one}
\usage{
as_dt_widget(x)
}
\description{
\code{\link[=TableModuleServer]{TableModuleServer()}} reaches into \verb{$x$options}/\verb{$x$data}/\verb{$x$selection}
etc, which only exist on the widget object \code{\link[DT:datatable]{DT::datatable()}} returns. A
bare data.frame passed through unchanged would silently corrupt itself --
each \code{dt$x$foo <- ...} builds a stray \code{x} list-column via \code{`$<-`} on a
data.frame that has no such column, which trips an opaque "replacement
has N rows, data has M" error instead of a clear one.
}
\keyword{internal}
