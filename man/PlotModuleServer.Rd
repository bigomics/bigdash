% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-module.R
\name{PlotModuleServer}
\alias{PlotModuleServer}
\title{Plot module server}
\usage{
PlotModuleServer(
  id,
  func,
  func2 = NULL,
  plotlib = "base",
  plotlib2 = plotlib,
  renderFunc = NULL,
  renderFunc2 = renderFunc,
  csvFunc = NULL,
  download.fmt = c("png", "pdf", "svg"),
  height = c(640, 800),
  width = c("auto", 1400),
  res = c(80, 110),
  download.pdf = NULL,
  download.png = NULL,
  download.html = NULL,
  download.csv = NULL,
  download.excel = NULL,
  download.obj = NULL,
  download.contrast.name = NULL,
  pdf.width = 8,
  pdf.height = 6,
  pdf.pointsize = 12,
  add.watermark = FALSE,
  remove_margins = FALSE,
  vis.delay = 3,
  card = NULL,
  parent_session = NULL,
  purge = NULL,
  purge.debug = FALSE
)
}
\arguments{
\item{id}{Module id, matching \code{\link[=PlotModuleUI]{PlotModuleUI()}}.}

\item{func, func2}{Functions returning the plot for the card and the modal.}

\item{plotlib, plotlib2}{Plotting library, see \code{\link[=PlotModuleUI]{PlotModuleUI()}}.}

\item{renderFunc, renderFunc2}{Override the render function inferred from \code{plotlib}.}

\item{csvFunc}{Function returning the underlying data, enabling CSV/Excel
download and click-to-label.}

\item{download.fmt}{Formats offered in the download menu.}

\item{height, width}{Length-2 vectors: card size, then modal size.}

\item{res}{Length-2 vector: card resolution, then modal resolution.}

\item{download.pdf, download.png, download.html, download.csv, download.excel, download.obj}{Override the generated \code{\link[shiny:downloadHandler]{shiny::downloadHandler()}}.}

\item{download.contrast.name}{Reactive appended to the download filename.}

\item{pdf.width, pdf.height, pdf.pointsize}{PDF export geometry.}

\item{add.watermark}{\code{FALSE}, or a position passed to the \code{bigdash.watermark_png}
/ \code{bigdash.watermark_pdf} hooks. See \code{\link[=bd_hook]{bd_hook()}}.}

\item{remove_margins}{Strip plot margins before export.}

\item{vis.delay}{Seconds to wait before screenshotting a visNetwork.}

\item{card}{Index of this plot when the UI was built with \code{cards = TRUE}.}

\item{parent_session}{Session of the calling module, needed by the editor.}

\item{purge}{Drop this plot's drawn DOM while it cannot be seen -- board
hidden, modal closed -- and redraw it on return. \code{NULL} (default) enables
it when the board has a \code{\link[=bd_visibility_probe]{bd_visibility_probe()}} and does nothing
otherwise; \code{TRUE}/\code{FALSE} force it. Only the client-side plotting
libraries are affected; \code{base}, \code{ggplot}, \code{grid} and \code{image} plots render
to a server-side image and are left alone.}

\item{purge.debug}{\code{message()} whether purging is on or off for this plot,
and every purge/redraw as it happens -- for the card, the maximise
modal, and the editor modal separately. \code{FALSE} by default.}
}
\description{
Plot module server
}
\seealso{
\code{\link[=bd_visibility_probe]{bd_visibility_probe()}}, which is what makes \code{purge} possible.
}
