% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visibility.R
\name{bd_is_visible}
\alias{bd_is_visible}
\title{Is this board on screen?}
\usage{
bd_is_visible(
  input,
  purge = TRUE,
  label = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{input}{Module \code{input}.}

\item{purge}{Enable dropping this board's drawn Plotly/iheatmapr trees while
it is hidden: \code{TRUE}, \code{FALSE}, or a reactive/function returning a logical
for a runtime switch. Only takes effect for plots that hold a redraw tick
(\code{\link[=PlotModuleServer]{PlotModuleServer()}} with \code{purge}, or \code{\link[=bd_redraw_tick]{bd_redraw_tick()}}).}

\item{label}{Optional label; when given, visibility changes are \code{message()}d.}

\item{session}{Module session.}
}
\value{
A \code{reactive} returning \code{TRUE} while the board is on screen.
}
\description{
Reads the \code{\link[=bd_visibility_probe]{bd_visibility_probe()}} embedded in the board's UI.
}
\section{What this is for}{

Not for gating plots and tables. Shiny already suspends hidden outputs
(\code{suspendWhenHidden = TRUE}), and a \code{reactive()} whose only consumers are
suspended outputs does not run either -- a hidden board is mostly free
without help. Use this flag for the things that escape that:
\itemize{
\item \code{observe()} / \code{observeEvent()}, which run whether or not anyone is looking
\item outputs set to \code{suspendWhenHidden = FALSE}
\item \code{invalidateLater()} polling
\item any reactive pulled eagerly by one of the above
}

The usual shape is \code{shiny::req(is_visible())} at the top of such an observer.
}

\examples{
\dontrun{
boardServer <- function(id) {
  moduleServer(id, function(input, output, session) {
    is_visible <- bd_is_visible(input)

    ## an observer would otherwise poll all session long
    observe({
      shiny::req(is_visible())
      shiny::invalidateLater(5000)
      refresh_status()
    })
  })
}
}

}
\seealso{
\code{\link[=bd_visibility_probe]{bd_visibility_probe()}}, \code{\link[=bd_redraw_tick]{bd_redraw_tick()}}
}
